import 'package:flutter/material.dart';

class JolusColors {
  // Brand Colors - Technological Store Branding
  static const primary = Color(0xFF0066FF); // Bright blue from the UI
  static const darkBlue = Color(0xFF00236F);    // Navy for titles
  static const background = Color(0xFFF8FAFF);  // Light grayish-blue background
  
  static const surface = Colors.white;
  static const surfaceLow = Color(0xFFF0F4F9);
  static const onSurfaceVariant = Color(0xFF65676B);
  static const outlineVariant = Color(0xFFE0E0E0);
  static const lightBlue = Color(0xFFE3F2FD);
  static const tertiary = Color(0xFF1F6FE5);

  static const error = Color(0xFFFF4D4D);
  static const success = Color(0xFF27AE60);
  static const warning = Color(0xFFF39C12); // Orange for pending
  
  static const textPrimary = Color(0xFF00236F);
  static const textSecondary = Color(0xFF65676B);
  static const textLight = Color(0xFF9E9E9E);
}
